# Entorno de DESARROLLO LOCAL (npm run dev / vite).
#
# CRIT-04 (auditoría seguridad 2026-09-21): antes este archivo apuntaba a
# producción real por descuido — cualquier `npm run dev` local creaba y
# modificaba productores reales y consumía cuota de la API de RENAPO.
# Ahora apunta al backend local (npm run dev en backend/, puerto 3000).
#
# ¿Necesitas probar contra producción o staging? Crea
# `frontend/.env.development.local` (NUNCA se sube a git, ver .gitignore)
# con VITE_API_URL apuntando ahí — no edites este archivo para eso.
VITE_API_URL=http://localhost:3000/api
VITE_VAPID_PUBLIC_KEY=BHJ83IcjVeMk85TQjvnFx2_LnkOQUHZHUh7fp0k-ODBkodcUaN54NDYK10huUrz3_MOihOgiXCmfzP-Oe9pc6ic
